#[allow(unused_imports)]
use links::{list::ListNode, tree::TreeNode};

fn main() {
    list_example();
    tree_example();
}

fn list_example() {
    //println!("Inserting values into list...");
    //let mut l = ListNode::new();
    //let mut tail = l.insert(1);
    //tail = tail.insert(2);
    //tail.insert(3);
    //
    //println!("List: {}", l);
    //l.reverse();
    //println!("List reversed: {}", l);
}

fn tree_example() {
    //println!("Inserting values into tree...");
    //let mut t = TreeNode::new();
    //let v = vec![0, 1, 2, -1, -2, -3];
    //for x in v {
    //    println!("=====================");
    //    t.insert(x);
    //    println!("{}", t.show(3));
    //    if !t.validate() {
    //      println!("Tree is not balanced!");
    //    }
    //}
}
